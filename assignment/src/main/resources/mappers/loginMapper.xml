<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace = "com.assignment.work.mapper.LoginMapper">
 	<select id="getLoginData" resultType="com.assignment.work.domain.Member">
 		select * from usertbl where id = #{id} and password = #{password}
 	</select>
 </mapper>